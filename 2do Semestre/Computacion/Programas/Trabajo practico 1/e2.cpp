#include<iostream>
#include<math.h>
using namesapde std;
int main ()
{
	int n,d;
	cout ( "introduzca dos enteros:" );
	scanf( "&d &d", &n, &d);
	if (n%d==0) print("%d es divisible por %d\n", n,d)
	system ('pause')
	return 0;
}
	
